def forecase_pipeline(type = 'Multivariate'):
  pass
  # Get the dataset

  # Check if dataset is up to date

  # If not fetch new data; add into MongoDB; train model; save model into S3 and local

  # If up to date, use local model to predict

  # Return the prediction
